# build output
dist/
# generated types
.astro/
# dependencies
node_modules/
# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
# environment variables
.env
.env.production
.env.*.local
# macOS / editor
.DS_Store
.vscode/
# vercel
.vercel

# local AI agents / environments
# Project-level Claude guidance (.claude/guidelines.md, settings.json) is
# versioned on purpose; only personal/local overrides are ignored.
.claude/settings.local.json
.gemini/

# local working docs and raw source assets (personal notes, third-party
# material, and the editable sources behind served files) — not for publishing.
# The optimized/served outputs live in public/; these are the originals.
_local/
my_info.md
PROJECTS.md
